name: "Nested action.yml run from workflows"

runs:
  using: composite
  steps:
    - name: simple step
      shell: bash
      run: |
        echo "Hello There!"
        echo $JF_ACCESS_TOKEN
        echo $JF_URL
